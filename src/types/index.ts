export type MenuItemType = {
    id: number
    title: string;
    icon: React.ReactNode;
    link: string;
 }

 export type HeadlineItemType = {
    id: number;
    title: string;
    description: string;
    image: string;
 }